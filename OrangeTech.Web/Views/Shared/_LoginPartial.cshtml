
@using (Html.BeginForm("Logout", "Account", FormMethod.Post, new { id = "logoutForm", @class = "nav-item" }))
{
    @if (User.Identity.IsAuthenticated)
    {
        <li nav-item><a href="javascript:document.getElementById('logoutForm').submit()" class="nav__link">Logout</a></li>
    }
}